import { ShortExchangeNamePipe } from './short-exchange-name.pipe';

describe('ShortExchangeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortExchangeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
